<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bzs.dao.MenuInfoMapper">
  <resultMap id="menu" type="com.bzs.model.MenuInfo">
    <!--
      WARNING - @mbg.generated
    -->
    <result column="MENU_ID" jdbcType="BIGINT" property="menuId" />
    <result column="PARENT_ID" jdbcType="BIGINT" property="parentId" />
    <result column="MENU_NAME" jdbcType="VARCHAR" property="menuName" />
    <result column="URL" jdbcType="VARCHAR" property="url" />
    <result column="ICON" jdbcType="VARCHAR" property="icon" />
    <result column="TYPE" jdbcType="CHAR" property="type" />
    <result column="ORDER_NUM" jdbcType="BIGINT" property="orderNum" />
    <result column="CREATE_TIME" jdbcType="TIMESTAMP" property="createTime" />
    <result column="MODIFY_TIME" jdbcType="TIMESTAMP" property="modifyTime" />
    <result column="PERMS" jdbcType="LONGVARCHAR" property="perms" />
  </resultMap>
  <select id="getUserPermissions" resultType="MenuInfo">
    select m.perms from role_info r
    left join account_role_info ar on(r.role_id = ar.role_id)
    left join account_info a on(a.account_id = ar.account_id)
    left join role_menu rm on(rm.role_id = r.role_id)
    left join menu_info m on(m.menu_id = rm.menu_id )
    where a.login_name = #{username} and m.perms is not null and m.perms &lt;&gt; ''
  </select>
</mapper>